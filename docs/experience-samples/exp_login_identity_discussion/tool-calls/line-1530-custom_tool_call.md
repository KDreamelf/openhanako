[2026-04-30 11:23:37] Codex: [工具调用] {"type":"custom_tool_call","status":"completed","call_id":"call_7iIas6MK9JsUpdW6cTrVT6pX","name":"apply_patch","input":"*** Begin Patch\n*** Update File: benchmarks/gpu_recovery_bench/gpu_recovery_bench.py\n@@\n-def validate_kernel(queue: cl.CommandQueue, program: cl.Program, password: bytes) -> None:\n+def validate_kernel(queue: cl.CommandQueue, kernel: cl.Kernel, password: bytes) -> None:\n@@\n-    program.pbkdf2_hmac_sha512_bench(\n+    kernel(\n         queue,\n         (1,),\n         None,\n@@\n def calibrate_batch(\n     queue: cl.CommandQueue,\n-    program: cl.Program,\n+    kernel: cl.Kernel,\n     password_buf: cl.Buffer,\n     password_len: int,\n     out_buf: cl.Buffer,\n@@\n     best_rate = 0.0\n     for batch_size in candidates:\n         start = time.perf_counter()\n-        event = program.pbkdf2_hmac_sha512_bench(\n+        event = kernel(\n             queue,\n             (batch_size,),\n             None,\n@@\n     ctx = cl.Context([device])\n     queue = cl.CommandQueue(ctx)\n     program = build_program(ctx)\n-    validate_kernel(queue, program, password)\n+    kernel = cl.Kernel(program, \"pbkdf2_hmac_sha512_bench\")\n+    validate_kernel(queue, kernel, password)\n@@\n     out_buf = cl.Buffer(ctx, mf.WRITE_ONLY, np.zeros(max_batch_size, dtype=np.uint64).nbytes)\n \n     if batch_size is None:\n-        batch_size = calibrate_batch(queue, program, password_buf, len(password), out_buf, max_batch_size, varied_input)\n+        batch_size = calibrate_batch(queue, kernel, password_buf, len(password), out_buf, max_batch_size, varied_input)\n \n     # Warm up the GPU and clocks.\n     for i in range(3):\n-        program.pbkdf2_hmac_sha512_bench(\n+        kernel(\n             queue,\n             (batch_size,),\n             None,\n@@\n     while time.perf_counter() < deadline:\n         batch_started = time.perf_counter()\n-        program.pbkdf2_hmac_sha512_bench(\n+        kernel(\n             queue,\n             (batch_size,),\n             None,\n*** End Patch\n"}
