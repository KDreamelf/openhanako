[2026-04-30 11:15:50] 工具: [工具输出] {"type":"function_call_output","call_id":"call_hE5cKGWWR8LTuJoUyMNsN2zy","output":"Chunk ID: 7139f4\nWall time: 0.3029 seconds\nProcess exited with code 0\nOriginal token count: 1176\nOutput:\n---\r\nname: uv-project-management\r\ndescription: 使用 uv 管理 Python 项目的完整工作流，包括安装 uv、用 uv 安装 Python、用 uv init 初始化项目、用 uv add 和 uv remove 维护依赖、用 uv sync 和 uv run 运行项目，以及把基于 requirements.txt、pip 或 venv 的旧项目整体迁移到 pyproject.toml 和 uv.lock 驱动的纯 uv 模式。当用户想编写 uv 运行指南、统一 Python 环境管理、把旧项目切到 uv、或排查 uv 常见使用问题时使用此技能。\r\n---\r\n\r\n# uv Project Management\r\n\r\n让 Python 项目的安装、运行、依赖维护和迁移都统一走 `uv`。\r\n\r\n## 核心原则\r\n\r\n1. 把 `uv` 当成唯一入口。\r\n2. 把 `pyproject.toml` 当成依赖声明源。\r\n3. 把 `uv.lock` 当成可复现环境的锁定结果。\r\n4. 让 `uv sync` 和 `uv run` 成为日常运行入口。\r\n5. 旧项目完成迁移后，不再把 `requirements.txt` 当主维护文件。\r\n\r\n## 先判断项目状态\r\n\r\n先在目标 Python 项目根目录确认当前属于哪一种：\r\n\r\n- 已有 `pyproject.toml` 和 `uv.lock`\r\n- 已有 `pyproject.toml`，但依赖仍主要写在 `requirements.txt`\r\n- 只有 `requirements.txt`，还没进入 `uv` 项目模式\r\n- 完全新项目，还没初始化\r\n\r\n按状态走下面的流程，不要混着用。\r\n\r\n## 一条龙运行流程\r\n\r\n### 1. 安装 uv\r\n\r\nWindows PowerShell：\r\n\r\n```powershell\r\npowershell -ExecutionPolicy ByPass -c \"irm https://astral.sh/uv/install.ps1 | iex\"\r\n```\r\n\r\n安装后重新打开终端，并确认：\r\n\r\n```powershell\r\nuv --version\r\n```\r\n\r\n### 2. 安装 Python\r\n\r\n优先显式安装目标版本：\r\n\r\n```powershell\r\nuv python install 3.12\r\n```\r\n\r\n如果用户没有指定版本，默认选当前项目可接受的稳定版本，并在文档里写清版本号。\r\n\r\n### 3. 准备项目环境\r\n\r\n#### 情况 A：已经是标准 uv 项目\r\n\r\n```powershell\r\nuv sync\r\n```\r\n\r\n#### 情况 B：旧项目，准备整体切到 uv 模式\r\n\r\n在项目根目录执行：\r\n\r\n```powershell\r\nuv init\r\nuv add -r requirements.txt\r\nuv sync\r\n```\r\n\r\n这条链路的含义：\r\n\r\n- `uv init`：生成 `pyproject.toml` 等 uv 项目基础文件\r\n- `uv add -r requirements.txt`：把旧依赖导入 `pyproject.toml`\r\n- `uv sync`：按当前声明和锁文件准备环境\r\n\r\n#### 情况 C：全新项目\r\n\r\n```powershell\r\nuv init\r\nuv add <依赖名>\r\nuv sync\r\n```\r\n\r\n### 4. 运行项目\r\n\r\n统一使用：\r\n\r\n```powershell\r\nuv run <命令>\r\n```\r\n\r\n常见示例：\r\n\r\n```powershell\r\nuv run python main.py\r\nuv run flask run\r\nuv run pytest\r\n```\r\n\r\n如果项目已经约定了启动命令，优先写项目自己的实际入口，不要凭空假设。\r\n\r\n## 纯 uv 维护方式\r\n\r\n### 新增依赖\r\n\r\n```powershell\r\nuv add flask requests\r\n```\r\n\r\n### 新增开发依赖\r\n\r\n```powershell\r\nuv add --dev pytest ruff\r\n```\r\n\r\n### 删除依赖\r\n\r\n```powershell\r\nuv remove requests\r\n```\r\n\r\n### 同步环境\r\n\r\n```powershell\r\nuv sync\r\n```\r\n\r\n### 运行命令\r\n\r\n```powershell\r\nuv run <命令>\r\n```\r\n\r\n### 升级依赖\r\n\r\n```powershell\r\nuv lock --upgrade-package requests\r\nuv sync\r\n```\r\n\r\n## 旧项目整体迁移流程\r\n\r\n当用户要把 `pip + venv + requirements.txt` 旧项目一口气切到纯 `uv` 模式时，优先给这条最短链路：\r\n\r\n```powershell\r\nuv python install 3.12\r\nuv init\r\nuv add -r requirements.txt\r\nuv sync\r\nuv run <实际启动命令>\r\n```\r\n\r\n迁移完成后，后续维护规则统一改成：\r\n\r\n- 新增依赖：`uv add`\r\n- 删除依赖：`uv remove`\r\n- 同步环境：`uv sync`\r\n- 运行项目：`uv run`\r\n- 锁定升级：`uv lock --upgrade-package`\r\n\r\n如果用户要求“完全不带历史包袱”，明确建议：\r\n\r\n1. 让 `pyproject.toml` 成为唯一主依赖入口。\r\n2. 让 `uv.lock` 成为环境锁定结果。\r\n3. 不再长期维护 `requirements.txt` 作为主文件。\r\n4. 不再把手动激活 `.venv` 当成标准流程。\r\n\r\n## 文档输出规则\r\n\r\n当用户要你写运行指南、部署指南或团队规范时，默认拆成两段：\r\n\r\n1. 使用者的一条龙运行流程\r\n2. 维护者的纯 uv 管理流程\r\n\r\n默认优先写这些命令：\r\n\r\n```powershell\r\nuv python install <版本>\r\nuv init\r\nuv add -r requirements.txt\r\nuv sync\r\nuv run <命令>\r\n```\r\n\r\n只有在项目已经明确完成迁移时，才把 `uv init` 和 `uv add -r requirements.txt` 从日常运行流程里拿掉，只保留：\r\n\r\n```powershell\r\nuv sync\r\nuv run <命令>\r\n```\r\n\r\n## 不要混用的旧习惯\r\n\r\n- 不要把 `pip install -r requirements.txt` 继续写成主流程\r\n- 不要长期同时维护两套依赖真相源\r\n- 不要默认要求用户手动激活虚拟环境\r\n- 不要把全局 Python 当项目环境\r\n- 不要在没确认项目入口前，擅自写死运行命令\r\n"}
